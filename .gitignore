# Dependencies (installed on demand by /design-setup)
node_modules/
package-lock.json

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Scratch / generated output (pages, shots, assets are written to /tmp at runtime)
/tmp/
*.log
.write-test

# anti-ai-eval / brand-lint run artifacts — they embed the scanned page's own copy
anti-ai-report.json
brand-lint.json
